To run evaluations on **HIDE** or **RealBlur**, run `metrics_batch.py`. To run evaluations on **GoPro**, run `metrics_dpdd.py`.

Remember to replace the value of `NAME`. Results are saved in `./{NAME}/metrics.csv`.

